# WEBCODE (fresh start)

Minimal app to upload an image and generate a QR that points at the hosted image.

Run locally:

1. npm install
2. npm start

Note: This is a fresh scaffold with a simple mock payment flow. Set `PAYSTACK_SECRET` to integrate a real gateway.
